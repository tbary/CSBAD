weights: "${project_dir}/models/${student}.pt"
name: "${dataset.name}_${student}_${teacher}_${teacher_strategy.name}"
entity: "tbary" #TO CHANGE for your WANDB entity
project: "YOLOv8" #TO CHANGE for your WANDB project
epochs: ${epochs}
batch: 16
data: "${project_dir}/experiments/model/data.yaml" 