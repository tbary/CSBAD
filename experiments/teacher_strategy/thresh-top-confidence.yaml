_target_: subsampling.teacher_strategies.teacher_thresholding_top_confidence

client_subsample_names: null # Filled by student strategy
teacher_image_label_path: "${dataset.bank_path}/labels_${teacher}_w_conf"
n: ${n_samples_teacher}
aggregation_function: "max"
name: "student-${strategy.name}-teacher-thresholded-top-confidence-${teacher_strategy.aggregation_function}-received-${n_samples}_stream_based_${n_samples_teacher}"